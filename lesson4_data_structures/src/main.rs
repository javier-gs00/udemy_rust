#![allow(dead_code)]

mod arrays;
mod enums;
mod option_t;
mod slices;
mod strings;
mod structures;
mod unions;
mod vectors;

fn main() {
    // structures::structures();
    // enums::enums();
    // unions::unions();
    // option_t::option_t();
    // arrays::arrays();
    // vectors::vectors();
    // slices::slices();
    strings::strings();
}
