#![allow(dead_code)]

mod arrays;
mod enums;
mod generics;
mod hashmaps;
mod option_t;
mod pattern_matching;
mod slices;
mod strings;
mod structures;
mod tuples;
mod unions;
mod vectors;

fn main() {
    // structures::structures();
    // enums::enums();
    // unions::unions();
    // option_t::option_t();
    // arrays::arrays();
    // vectors::vectors();
    // slices::slices();
    // strings::strings();
    // tuples::tuples();
    // hashmaps::entry();
    // pattern_matching::pattern_matching();
    generics::generics();
}
