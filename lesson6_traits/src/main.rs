#![allow(dead_code)]

mod traits;
mod traits_and_parameters;
mod into;
mod drop;

fn main() {
    // traits::enter();
    // traits_and_parameters::enter();
    // into::enter();
    drop::enter();
}
